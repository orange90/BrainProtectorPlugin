# Changelog

本项目所有显著变更将记录在本文件中。版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [2.0.0] - 2026-06-11

本次为 **大版本升级**：新标签页由单一面板重构为「时间管理 / 任务管理 / 状态管理」三 Tab 架构，并新增专注期硬拦截、AI 行为洞察两大子系统。

### ✨ 新增功能

- **三 Tab 主面板**：新标签页 / 工具栏弹窗统一为「时间管理 / 任务管理 / 状态管理」可滑动切换的三 Tab 布局。
- **任务管理（标签页整理）**：
  - 读取全窗口所有 Tab，按域名 / 类别归类，支持展开收起与单击关闭。
  - 自动标记 > 12 小时未访问的「陈旧标签」与重复打开的 URL。
  - 支持标题 / 域名搜索 + 「全部 / 陈旧 / 重复」分段筛选。
  - 「一键应用为 Chrome 标签组」：将当前归类同步为浏览器原生标签组并按类别上色。
- **状态管理**：
  - 🎯 **意图锚**：每轮专注开始前写下 implementation intention，结束时弹窗结算「完成 / 未完成」并按天留存历史。
  - 🚦 **实时信号灯**：疲劳、多任务、专注一致、干扰命中 4 张自动信号卡。
  - 🔁 **自动切换检测**：后台监听 `tabs.onActivated` / `windows.onFocusChanged`，按 90s 窗口去重写入日志。
  - 💡 **实时建议**：根据信号灯与认知负荷自动生成 3~5 条可执行建议。
- **🛑 专注期硬拦截（focus-block.js）**：
  - 开启番茄 / 深度 / 自定义专注后，访问设置的「分心域名」时注入 **Shadow DOM 全屏遮罩**，并强制 5 秒倒计时。
  - 三个出口：「我必须现在用」（倒计时归零后启用，本次会话内放行）/「回到上一页」/「关闭此页」。
  - 设置页提供一键填入常见分心域名（社交 / 视频 / 内容站）的推荐清单。
  - 监听 `chrome.storage` 与 SPA 路由变化实时联动，停止计时器或移出清单即刻撤掉遮罩。
- **🤖 AI 行为洞察**：
  - 新增 [ai-analyzer.js](src/ai-analyzer.js)：把本地行为指标聚合为紧凑 JSON，调用任意 **OpenAI Chat Completions 兼容接口** 生成 Markdown 报告。
  - 支持自选时间窗口（今天 / 3 / 7 / 14 / 30 天）。
  - 报告自动渲染 Mermaid 图表（离线 `vendor/mermaid.min.js`）。
  - 设置页新增「AI 行为分析」配置区：接口地址 / API Key（带显隐切换）/ 模型 / 温度 / 系统提示词，密钥仅本地保存。
- **📝 在线文档创作检测扩展**：
  - 新增 [online-docs.js](content-scripts/sites/online-docs.js)，覆盖 Google Docs、飞书 / Lark / Larkoffice、语雀、Notion、腾讯文档、企业微信文档。
  - canvas 编辑器按 `all_frames` 注入、按键节奏判定；腾讯文档 / 企业微信文档采用域名级聚焦判定。
  - 在线文档子域与主站分开统计。
- **📅 完整报表升级**：
  - 新增「本周周报」：KPI 卡片 + 易分心时段小时热力图 + 最分心域名 Top + 最长连续无中断段 Top 5。
  - 「近 7 天趋势」支持切换指标（总时长 / 浏览 / 创作 / 专注 / 切换次数等）。
- **🔁 接管开关**：新标签页右上角可一键关闭新标签页接管，仍能从工具栏图标弹窗打开；首次安装时弹窗征询。
- **🍅 专注计时器持久化**：以墙钟终点为锚点，跨刷新 / 新标签页继续走动。
- **完成提醒可选**：勾选「完成后提醒」才在归零时弹窗 / 发系统通知。

### 🛠️ 改进

- 新 UI：冷感极简 · 石墨蓝 + 微玻璃质感，强调色改亮橙，Tab 居中。
- 新版图标（icon16 / 48 / 128）全部更换。
- options 页内联 theme 脚本抽离为 `theme-init.js`，修复 CSP 违规。
- 分类规则 `rules/categories.json` 调整，覆盖更多在线文档子域。
- 通用计时与认知负荷量化基于客观信号，无需手动打勾。
- 引入本地 `vendor/marked.min.js` 与 `vendor/mermaid.min.js`，AI 报告完全离线渲染。

### 🔒 权限与隐私

- 新增 `alarms`、`tabGroups` 权限以支持持久化计时器与标签组应用。
- 默认仍 **无任何网络请求**；唯一一处可选联网是用户手动点击「开始 AI 分析」时调用自配 API。

### 🧱 工程

- `manifest.json` 升级 `version` 至 `2.0.0`。
- 新增 `content-scripts/focus-block.js`、`src/ai-analyzer.js`、`icons/logo.png`、`vendor/` 等文件。
- 架构由三层扩展为四层：Dashboard / 通用层 / 深度感知层 / 专注拦截层。

---

## [1.6.0] - 2025-xx

- 新标签页接管征询与开关 + 弹窗加宽。

## [1.5.0]

- 知乎深度感知；专注计时器、有害行为自检、认知负荷分析；完整报表与设置页等首批能力。
